# AFMX — production pinned dependencies
# All versions are exact pins for reproducible production deployments.
# Generated against Python 3.10 on Oracle Cloud Linux / Ubuntu 22.04.
#
# Update process:
#   pip-compile requirements.txt -o requirements-prod.txt
#   OR manually update after testing against the test suite.
#
# DO NOT install dev tools (pytest, black, ruff, mypy) in production.

fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.2.1
python-multipart==0.0.9
python-dotenv==1.0.1
httpx==0.27.0
aiohttp==3.9.5
redis[asyncio]==5.0.4
hiredis==2.3.2
prometheus-client==0.20.0
orjson==3.10.3
